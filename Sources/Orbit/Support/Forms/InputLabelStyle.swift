import SwiftUI

/// Style variant for Orbit input label.
public enum InputLabelStyle {

    /// Style with label positioned above the input.
    case `default`
    /// Style with compact label positioned inside the input.
    case compact
}
